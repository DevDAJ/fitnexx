import { Header } from "@/components/app/Header";

export default function MacrosPage() {
  return (
    <div className="flex-1 p-4 sm:px-6">
      <Header
        title="Macros"
        description="Track your daily calorie and macro intake locally in your browser — data never leaves your device."
      />
    </div>
  );
}
